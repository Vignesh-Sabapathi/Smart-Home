<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HomeAutomation" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="statusBarBackground">@color/darkblue</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Custom.Widget.SwitchCompat" parent="Widget.AppCompat.CompoundButton.Switch">
        <item name="android:textColorPrimary">@color/avgblue</item>
        <item name="colorControlActivated">@color/avgblue</item>
    </style>
</resources>